#version 400 core

in vec3 position;

// cannot use the textureCoordinates in the vertex shader
in vec2 textureCoordinates;
out vec2 pass_textureCoordinates;

void main(void) {
    gl_Position = vec4(position, 1.0);
    pass_textureCoordinates = textureCoordinates;
}